interface:
  display_name: "Library"
  short_description: "Save chat context as a library note"
  default_prompt: "Use $library to save this chat to the library as a numbered Markdown note."
policy:
  allow_implicit_invocation: false
