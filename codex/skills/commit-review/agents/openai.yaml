interface:
  display_name: "Commit Review"
  short_description: "Review local diffs or commits"
  default_prompt: "Use $commit-review to review my staged diff and write the report to .local.docs."

policy:
  allow_implicit_invocation: false
